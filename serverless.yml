# Welcome to Serverless!
#
# This file is the main config file for your service.
# It's very minimal at this point and uses default values.
# You can always add more config options for more control.
# We've included some commented out config examples here.
# Just uncomment any of them to get that config option.
#
# For full config options, check the docs:
#    docs.serverless.com
#
# Happy Coding!

service: majestic-contact-form
# app and org for use with dashboard.serverless.com
#app: your-app-name
#org: your-org-name

# You can pin your service to only deploy with a specific Serverless version
# Check out our docs for more details
# frameworkVersion: "=X.X.X"

provider:
  name: aws
  runtime: nodejs12.x
  memorySize: 256
  timeout: 300
  region: eu-west-1
  deploymentBucket:
    name: ${env:SERVERLESS_DEPLOYMENT_BUCKET, 'majestic-private'}
  iamRoleStatements:
    -  Effect: "Allow"
       Action:
         - "s3:PutBucketNotification"
       Resource:
         - arn:aws:s3:::${env:SERVERLESS_DEPLOYMENT_BUCKET, 'majestic-private'}
  httpApi:
    cors: false
  logs:
    httpApi: true
package:
  exclude:
    - .*/**
  include:
    - contact.js
    - node_modules/**

plugins:
  - serverless-prune-plugin
#  - serverless-domain-manager


functions:
  contact:
    handler: src/handlers/contact.handler
    name: majestic-contact
    role: contactRole
    timeout: 28
    environment:
      REDIRECT_LOCATION: ${env:MAJESTIC_REDIRECT_LOCATION, 'https://www.majestic-motors.co.uk/thanks'}
      CONTACT_DESTINATION: info@majestic-motors.co.uk
      MONDAY_API_V2_TOKEN: ${env:MAJESTIC_MONDAY_API_V2_TOKEN}
      SENDER: info@majestic-motors.co.uk
    events:
      - httpApi:
          method: POST
          path: /contact
  create-gdoc:
    handler: src/handlers/gdoc.create
    name: majestic-gdocs
    role: gdocRole
    timeout: 28
    environment:
      MONDAY_API_V2_TOKEN: ${env:MAJESTIC_MONDAY_API_V2_TOKEN}
      MAJESTIC_MONDAY_APP_SIGNING_SECRET: ${env:MAJESTIC_MONDAY_APP_SIGNING_SECRET}
      ON_ERR_EMAIL: info@majestic-motors.co.uk
    events:
      - httpApi:
          method: POST
          path: /gdocs
resources:
  Resources:
    gdocRole:
      Type: "AWS::IAM::Role"
      Properties:
        AssumeRolePolicyDocument:
          Version: "2012-10-17"
          Statement:
            - Effect: "Allow"
              Principal:
                Service:
                  - "lambda.amazonaws.com"
              Action: "sts:AssumeRole"
        ManagedPolicyArns:
          - arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole
          - arn:aws:iam::aws:policy/service-role/AWSLambdaENIManagementAccess
        Policies:
          - PolicyName: "DynamoDB"
            PolicyDocument:
              Version: "2012-10-17"
              Statement:
                - Effect: "Allow"
                  Action: "dynamodb:*"
                  Resource: "arn:aws:dynamodb:eu-west-1:*:table/AtomicCounters"
          - PolicyName: "EnableLogging"
            PolicyDocument:
              Version: "2012-10-17"
              Statement:
                - Effect: "Allow"
                  Action:
                    - "logs:CreateLogGroup"
                    - "logs:CreateLogStream"
                    - "logs:PutLogEvents"
                  Resource: "arn:aws:logs:*:*:*"
          - PolicyName: "SendEmail"
            PolicyDocument:
              Version: "2012-10-17"
              Statement:
                - Effect: "Allow"
                  Action:
                    - "ses:SendEmail"
                    - "ses:SendRawEmail"
                  Resource: "*"
        RoleName: majestic-api-GDocsRole
    contactRole:
      Type: "AWS::IAM::Role"
      Properties:
        AssumeRolePolicyDocument:
          Version: "2012-10-17"
          Statement:
            - Effect: "Allow"
              Principal:
                Service:
                  - "lambda.amazonaws.com"
              Action: "sts:AssumeRole"
        ManagedPolicyArns:
          - arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole
          - arn:aws:iam::aws:policy/service-role/AWSLambdaENIManagementAccess
        Policies:
          - PolicyName: "EnableLogging"
            PolicyDocument:
              Version: "2012-10-17"
              Statement:
                - Effect: "Allow"
                  Action:
                    - "logs:CreateLogGroup"
                    - "logs:CreateLogStream"
                    - "logs:PutLogEvents"
                  Resource: "arn:aws:logs:*:*:*"
          - PolicyName: "SendEmail"
            PolicyDocument:
              Version: "2012-10-17"
              Statement:
                - Effect: "Allow"
                  Action:
                    - "ses:SendEmail"
                    - "ses:SendRawEmail"
                  Resource: "*"
        RoleName: majestic-api-ContactRole

